# LOCKED IN V3

Offline-first personal operating system for training, nutrition, mind, life, squad and glow-up progress. The app is plain HTML/CSS/JS: no build step, no CDN, no account and no cloud dependency.

This first publish contains the app shell and staged module loader. Open `index.html` directly to run it; later commits add the full local UI.
