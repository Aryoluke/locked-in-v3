(function(){'use strict';function install(){const app=window.app;if(!app)return;const add=()=>{const root=document.getElementById('app');if(!root||!location.hash.includes('advanced')||root.querySelector('[data-action="edit-profile"]'))return;const card=root.querySelector('.card');if(!card)return;const box=document.createElement('div');box.className='action-grid';box.innerHTML='<button type="button" class="btn" data-action="edit-profile">Edit profile and re-onboard</button>';card.appendChild(box)};document.addEventListener('click',e=>{const n=e.target.closest('[data-action="edit-profile"]');if(!n)return;const s=app.state(),name=window.prompt('Profile name',s.profile.name||'Operator');if(name===null)return;s.profile.name=name.trim()||'Operator';s.onboardingComplete=true;app.save();app.render();app.toast('Profile updated locally')});window.addEventListener('hashchange',()=>setTimeout(add,0));setTimeout(add,0)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install()})();
