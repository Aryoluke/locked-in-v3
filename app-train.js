(()=>{const L=window.LI,esc=L.esc;let timer=null,seconds=90;const V=window.LIViews;V.train=()=>{const s=L.state,logs=s.logs.filter(x=>x.area==='train'),all=L.SEED.exercises.concat(s.customExercises||[]);return `<div class="hero"><div><div class="eyebrow">MOVE / BUILD / RECOVER</div><h1>Training <span class="accent">command.</span></h1><p class="muted">Adaptive sessions, honest logs and progressive overload.</p></div><button class="btn btn-primary" data-action="start-workout">Start session</button></div><div class="grid grid-4"><div class="card"><div class="stat">${logs.length}</div><div class="stat-label">logged sets</div></div><div class="card"><div class="stat">${s.records.length}</div><div class="stat-label">PRs</div></div><div class="card"><div class="stat">${Math.max(0,100-logs.length*4)}%</div><div class="stat-label">muscle freshness</div></div><div class="card"><div id="rest-time" class="stat">01:30</div><div class="stat-label">rest timer</div><button class="btn btn-ghost" data-action="rest">Start / reset</button></div></div><div class="grid grid-2 section"><div class="card"><div class="section-head"><h3>Log a set</h3><span class="tag">1RM + fatigue</span></div><div class="form-grid"><label>Exercise<select id="ex-name">${all.map(x=>`<option>${esc(x[0]||x.name)}</option>`).join('')}</select></label><label>Variation<input id="ex-var" placeholder="Paused, tempo, unilateral..."></label><label>Weight (kg)<input id="ex-weight" type="number" value="60"></label><label>Reps<input id="ex-reps" type="number" value="8"></label><label>RPE<input id="ex-rpe" type="number" min="1" max="10" value="8"></label></div><button class="btn btn-primary" data-action="log-set">Log set + PR check</button></div><div class="card"><div class="section-head"><h3>Today’s template</h3><span class="tag">${s.profile.equipment}</span></div><div class="list">${L.SEED.workouts.map(w=>`<div class="list-row"><span><b>${w.name}</b><br><span class="muted small">${w.duration} min · ${w.items.join(' · ')}</span></span><button class="btn btn-ghost" data-action="use-template" data-name="${esc(w.name)}">Use</button></div>`).join('')}</div></div></div><div class="section card"><div class="section-head"><h3>Library & progression</h3><button class="btn btn-ghost" data-action="add-exercise">+ Custom exercise</button></div><div class="chips">${all.map(x=>`<button class="chip" data-action="exercise-info" data-name="${esc(x[0]||x.name)}">${esc(x[0]||x.name)} · ${esc(x[1]||x.muscle)}</button>`).join('')}</div><div class="section-head" style="margin-top:22px"><h3>Muscle fatigue map</h3><span class="tag">last 7 days</span></div><div class="grid grid-3">${['Chest','Back','Legs','Shoulders','Core','Arms'].map((m,i)=>`<div><div class="metric-row"><span>${m}</span><b>${[34,48,66,28,51,21][i]}%</b></div><div class="progress"><i style="width:${[34,48,66,28,51,21][i]}%"></i></div></div>`).join('')}</div></div><div class="grid grid-2 section"><div class="card"><h3>Warm-up & mobility</h3><div class="chips">${['Hip flow 6m','Shoulder CARs','Ankle rocks','90/90 reset','Couch stretch'].map(x=>`<button class="chip" data-action="mobility" data-name="${x}">${x}</button>`).join('')}</div><h3 style="margin-top:20px">Skill progressions</h3><div class="list">${L.SEED.skills.map((x,i)=>`<div class="list-row"><span>${x}</span><span class="gold">${i+1}/5</span></div>`).join('')}</div></div><div class="card"><h3>Training history</h3><div class="timeline">${logs.slice(-5).reverse().map(x=>`<article><b>${esc(x.exercise)}</b><div class="muted small">${x.weight}kg × ${x.reps} · RPE ${x.rpe} · ${new Date(x.at).toLocaleDateString()}</div></article>`).join('')||'<div class="empty">Your first set starts the timeline.</div>'}</div><button class="btn btn-ghost" data-action="body-scan">Add body scan / photo note</button></div></div>`};
function action(a,b){const s=L.state;if(a==='rest'){if(timer){clearInterval(timer);timer=null}seconds=90;const el=document.querySelector('#rest-time');timer=setInterval(()=>{seconds--;if(el)el.textContent=`00:${String(seconds).padStart(2,'0')}`;if(seconds<=0){clearInterval(timer);timer=null;beep();}},1000)}if(a==='log-set'){const weight=+$('#ex-weight').value||0,reps=+$('#ex-reps').value||0,exercise=$('#ex-name').value,one=weight*(1+reps/30);s.logs.push({area:'train',exercise,variation:$('#ex-var').value,weight,reps,rpe:+$('#ex-rpe').value,oneRM:Math.round(one),at:new Date().toISOString()});const old=s.records.find(x=>x.exercise===exercise);if(!old||one>old.oneRM){s.records=s.records.filter(x=>x.exercise!==exercise);s.records.push({exercise,oneRM:Math.round(one),at:new Date().toISOString()});toast('NEW PR · '+Math.round(one)+'kg estimated 1RM')}else toast('Set logged +20 XP');s.xp+=20}if(a==='use-template'){s.xp+=10;toast(b.dataset.name+' loaded into today')}if(a==='mobility'){s.tasks.push({kind:'mobility',name:b.dataset.name,at:new Date().toISOString()});s.xp+=8;toast(b.dataset.name+' added')}if(a==='start-workout'){toast('Session started · warm up first')}if(a==='add-exercise'){const n=prompt('Custom exercise name');if(n){s.customExercises.push([n,'Custom','Custom']);toast('Exercise added')}}if(a==='exercise-info')toast(b.dataset.name+' · choose a controlled variation and log RPE')}function beep(){try{const c=new AudioContext(),o=c.createOscillator();o.connect(c.destination);o.frequency.value=880;o.start();o.stop(c.currentTime+.25)}catch(e){}toast('REST OVER · next set')}function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2200)}document.addEventListener('click',e=>{if(e.target.dataset?.action?.match(/^(rest|log-set|use-template|mobility|start-workout|add-exercise|exercise-info|body-scan)$/)){if(e.target.dataset.action==='body-scan'){const n=prompt('Body scan note or photo filename (stored locally)');if(n){L.state.bodyLogs.push({note:n,at:new Date().toISOString()});L.save();toast('Body timeline updated')}}else{action(e.target.dataset.action,e.target);L.save();if(L.route==='train')L.render()}}});})();
